# This CRD is automatically generated from the helm charts, do not modify! 
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: jupyterservers.amalthea.dev
spec:
  group: amalthea.dev
  names:
    kind: JupyterServer
    plural: jupyterservers
    shortNames:
    - js
    singular: jupyterserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The Jupyter server image that is running
      jsonPath: .spec.jupyterServer.image
      name: Image
      type: string
    - description: Full URL where the server can be reached
      jsonPath: .status.create_fn.fullServerURL
      name: URL
      type: string
    - description: Status of the main pod
      jsonPath: .status.mainPod.status.phase
      name: Pod Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              auth:
                default: {}
                properties:
                  oidc:
                    default: {}
                    properties:
                      authorizedEmails:
                        default: []
                        description: 'List of users (identified by Email address read
                          from the "email" OIDC claim)

                          which are allowed to access this Jupyter session. This list
                          is stored as a file

                          and passed to the `--authenticated-emails-file` option (see

                          https://oauth2-proxy.github.io/oauth2-proxy/docs/configuration/overview#command-line-options).

                          '
                        items:
                          type: string
                        type: array
                      authorizedGroups:
                        default: []
                        description: 'List of groups of users (read from the "groups"
                          OIDC claim) which are allowed

                          to access this Jupyter session. This list passed to the
                          `--allowed-group` option (see

                          https://oauth2-proxy.github.io/oauth2-proxy/docs/configuration/overview#command-line-options).

                          '
                        items:
                          type: string
                        type: array
                      clientId:
                        type: string
                      clientSecret:
                        description: 'The client secret of the application registered
                          with the OIDC provider. This

                          secret can be given here explicitly as string or through
                          a reference to an

                          existing secret. Using the secret reference is the preferred
                          option because it

                          avoids storing the secret in cleartext on the custom resource
                          specification.

                          '
                        oneOf:
                        - required:
                          - value
                        - required:
                          - secretKeyRef
                        properties:
                          secretKeyRef:
                            description: 'A regular reference to the key/secret which
                              holds the client secret of the

                              application registered with the OIDC provider. Note
                              that the secret has to

                              be in the same namespace in which the custom resource
                              object is going to be

                              created.

                              '
                            properties:
                              key:
                                type: string
                              name:
                                type: string
                            type: object
                          value:
                            type: string
                        type: object
                      enabled:
                        default: false
                        type: boolean
                      issuerUrl:
                        type: string
                    type: object
                  token:
                    description: "A token that will be passed to the `--ServerApp.token`\
                      \ option when running\nthe Jupyter server and needed when first\
                      \ accessing the Jupyter server. The\noptions are:\n- By leaving\
                      \ this field empty, a token will be autogenerated and added\
                      \ under the\n  key `ServerApp.token` to the secret which is\
                      \ created as a child of the custom\n  resource object.\n- Setting\
                      \ the token to an empty string \"\" runs the Jupyter server\
                      \ container\n  itself without any authentication. This is recommended\
                      \ when enabling OIDC\n  as authentication and authorization\
                      \ are then handled by the dedicated plugins.\n- Set an actual\
                      \ value here. Note that this string will be stored in clear\
                      \ text\n  as part of the custom resource object. This option\
                      \ is mostly useful for dev\n  purposes.\n"
                    type: string
                type: object
              culling:
                default: {}
                description: Options about culling idle servers
                properties:
                  idleSecondsThreshold:
                    default: 0
                    description: 'How long should a server be idle for before it is
                      culled. A value of zero

                      indicates that the server should never be culled.

                      '
                    minimum: 0
                    type: integer
                type: object
              jupyterServer:
                default: {}
                properties:
                  defaultUrl:
                    default: /lab
                    type: string
                  image:
                    default: jupyter/minimal-notebook:latest
                    type: string
                  resources:
                    default: {}
                    description: 'Regular resource requests, will be set on the main
                      notebook

                      container.

                      '
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  rootDir:
                    default: /home/jovyan/work
                    description: 'The absolute path to the root/notebook directory
                      for jupyterlab. Should

                      lead to a subdirectory of or match the path at storage.pvc.mountPath

                      '
                    type: string
                type: object
              patches:
                default: []
                description: 'Patches to be applied. Currently only json patches and
                  json merge

                  patches are supported.

                  '
                items:
                  properties:
                    patch:
                      x-kubernetes-preserve-unknown-fields: true
                    type:
                      enum:
                      - application/json-patch+json
                      - application/merge-patch+json
                      type: string
                  type: object
                type: array
              routing:
                default: {}
                properties:
                  host:
                    type: string
                  ingressAnnotations:
                    default: {}
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  path:
                    default: /
                    type: string
                  tls:
                    default: {}
                    properties:
                      enabled:
                        default: false
                        type: boolean
                      secretName:
                        type: string
                    type: object
                type: object
              storage:
                default: {}
                properties:
                  pvc:
                    default: {}
                    properties:
                      enabled:
                        default: false
                        description: 'Wether a PVC should be used to back the session.
                          Defaults

                          to ''false'' in which case an emptyDir volume will be used.

                          '
                        type: boolean
                      mountPath:
                        default: /home/jovyan/work
                        description: 'The absolute path to the location where the
                          PVC should be

                          mounted in the user session pod.

                          '
                        type: string
                      storageClassName:
                        description: 'Storage class to be used for the PVC. If left
                          empty, the

                          default storage class defined for the cluster will be used.

                          '
                        type: string
                    type: object
                  size:
                    default: 100Mi
                    description: 'Size of the PVC or sizeLimit of the emptyDir volume
                      which

                      backs the session respectively.

                      '
                    type: string
                type: object
            type: object
          status:
            default:
              children: {}
              mainPod: {}
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
