apiVersion: amalthea.dev/v1alpha1
kind: JupyterServer
metadata:
  name: ## ADD NAME
  namespace: ## ADD NAMESPACE
spec:
  type: jupyterlab
  routing:
    host: ## Add hostname
  auth:
    token: ## Add a token here
