kind: Service
apiVersion: v1
metadata:
  name: {name}
  labels:
    app: {name}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  selector:
    app: {name}
  ClusterIP: None
